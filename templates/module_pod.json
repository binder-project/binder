{
  "kind": "Pod",
  "apiVersion": "v1",
  "metadata": {
    "name": "{{component.name}}",
    "labels": {
      "name": "{{component.name}}",
      "app_id": "{{app.id}}"
    }
  },
  "spec": {
    "containers": [
      {{containers}}
    ]
  }
}
