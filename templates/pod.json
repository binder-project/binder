{
  "kind": "Pod",
  "apiVersion": "v1",
  "metadata": {
    "name": "{{app.name}}",
    "labels": {
      "name": "{{app.name}}",
      "app_id": "{{app.id}}"
    }
  },
  "spec": {
    "containers": [
      {{containers}}
    ]
  }
}
