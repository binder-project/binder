{
  "kind": "Pod",
  "apiVersion": "v1",
  "metadata": {
    "name": "{{component.name}}",
    "labels": {
      "name": "{{component.name}}"
    }
  },
  "spec": {
    "containers": [
      {{containers}}
    ]
  }
}
