{
  "kind": "Service",
  "apiVersion": "v1",
  "metadata": {
    "name": "{{component.name}}",
    "labels": {
      "name": "{{component.name}}",
      "app_id": "{{app.id}}"
    }
  },
  "spec": {
    "ports": [
      {
        "port": {{app.service_port}},
        "targetPort": {{component.pod_port}}
      }
    ],
    "selector": {
      "name": "{{component.name}}"
    }
  }
}
